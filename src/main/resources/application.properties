spring.application.name=ProgrammationTravaux
server.port=8095

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:validate}
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jackson.serialization.write-dates-as-timestamps=false

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB

spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${KEYCLOAK_JWK_URI}

keycloak.token-url=${KEYCLOAK_TOKEN_URL}
keycloak.client-id=${KEYCLOAK_CLIENT_ID}
keycloak.client-secret=${KEYCLOAK_CLIENT_SECRET}

cors.allowed-origins=${CORS_ORIGINS}
file.upload-dir=${FILE_UPLOAD_DIR:./uploads}

spring.main.allow-bean-definition-overriding=true
spring.flyway.enabled=false
spring.servlet.multipart.file-size-threshold=2MB
server.servlet.session.timeout=120m

keycloak.admin-base-url=${KEYCLOAK_ADMIN_BASE_URL:http://localhost:8080}
keycloak.realm-name=${KEYCLOAK_REALM_NAME:programmation-travaux}

spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=100,expireAfterWrite=60s
# true en production HTTPS, false en développement local HTTP
app.cookie-secure=${COOKIE_SECURE:false}

# Evolution V3 idempotente avant validation Hibernate
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema-v3.sql
spring.sql.init.continue-on-error=false
spring.jpa.defer-datasource-initialization=false

# Web Push VAPID (clés générées et persistées automatiquement si absentes)
app.vapid.subject=${VAPID_SUBJECT:mailto:admin@sonabel.bf}
app.vapid.keys-file=${VAPID_KEYS_FILE:./.vapid-keys.properties}
